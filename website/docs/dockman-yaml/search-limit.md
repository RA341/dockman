# File Search limit

:::note
Available v2.6+
:::

You can set a limit on the search results when searching files default is 5. Change it by setting the following in
.dockman.yaml

```yaml
searchLimit: 5
```
